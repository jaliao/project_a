## 1. 樣式調整

- [x] 1.1 修改 `components/course-session/course-status-badge.tsx`，將 `STATUS_COLORS.recruiting` 由 `bg-gray-100 text-gray-600` 改為 `bg-yellow-100 text-yellow-700`

## 2. 驗證

- [x] 2.1 於瀏覽器檢視招生中課程卡片／清單，確認「招生中」標籤底色已變為黃色，且與進行中（藍）、已結業（綠）、已取消（紅）標籤並列時樣式一致（`{color}-100`／`{color}-700`）
